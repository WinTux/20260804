apiVersion: v1
kind: Service
metadata:
  name: principal-nodeport-service
spec:
  type: NodePort
  selector:
    app: principalservice
  ports:
    - name: principalservice
      protocol: TCP
      port: 8080
      targetPort: 8080
    